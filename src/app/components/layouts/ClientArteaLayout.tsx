"use client";

const ClientArteaLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <main className="main client-area-bg">
      {children}
    </main>
  )
};

export default ClientArteaLayout;
