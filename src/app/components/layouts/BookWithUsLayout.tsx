"use client";

const BookWithUsLayout = ({ children }: { children: React.ReactNode }) => {
  return (
    <main className="main book-with-us-page-bg">
      {children}
    </main>
  )
};

export default BookWithUsLayout;
